import * as THREE from "three";

export class Game {
    constructor(scene, camera, light) {
        this.scene = scene;
        this.camera = camera;
        this.light = light;
    }

    update() {
    }
}